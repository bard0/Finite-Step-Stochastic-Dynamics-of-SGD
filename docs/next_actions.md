# Next Actions

## Cheap falsification gates first

1. **Finite-sum replication of M9.** Reproduce the `G_Sigma` threshold separation using genuine minibatch sampling, with matched zero-order descriptors and no continuous-noise shortcut.
2. **Coefficient audit.** Verify the exact `eta_c` formula and scaling across `lambda`, batch size, and controlled noise-geometry strength before moving to neural models.
3. **Multidimensional control.** Test noncommuting Hessian/noise geometry against an exact lifted second-moment or Lyapunov/LMI baseline.

## Medium-cost work

4. Rebuild the neural predictive protocol with independent states/conditions, held-out targets, preregistered directions, and complete baseline hierarchy.
5. Test whether a compact noise-geometry observable adds held-out predictive value beyond GNS, sharpness, `eta*lambda_max`, and projected covariance.

## Theory

6. Finish the literature/priority audit for the order-4/order-5 covariance-jet hierarchy before claiming novelty.
7. Formalize the finite-sample stability-boundary problem and try to falsify the non-regularity hypothesis on the exactly solvable quadratic finite-sum model.

## Stop rules

- Do not escalate to large neural experiments if the finite-sum replication fails.
- Do not claim a new higher-cumulant mechanism without a distinct observable consequence and literature separation.
- Do not treat point operator accuracy as sufficient near a stability boundary; report uncertainty.
