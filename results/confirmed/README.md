# Confirmed results

Compact public evidence for results that passed the project validation criteria.

- **M1 — finite-horizon covariance under i.i.d. minibatching.** Zero-fit covariance prediction from local anisotropic gradient noise propagated through deterministic geometry is supported in the tested small-network regime. See `../../experiments/M1/results/`.
- **M2 — random-reshuffling temporal covariance.** The completed run returned diagnosis M2-1: temporal covariance information improved finite-horizon covariance prediction beyond a matched one-step without-replacement baseline in the tested small-CNN regime. See `../../experiments/M2/results/decision_report.md`.

These claims remain scoped to the evaluated architectures, datasets, horizons and sampling rules. Large raw artifacts are stored separately.
